{
  "name": "gatsby-plugin-relative",
  "description": "Add slug (URI) to selected gatsby nodes",
  "version": "3.0.0",
  "author": "Act Labs <act7labs@gmail.com>",
  "scripts": {
    "build": "babel src --out-dir . --ignore **/__tests__",
    "prepare": "cross-env NODE_ENV=production npm run build",
    "watch": "babel -w src --out-dir . --ignore **/__tests__",
    "test": "jest"
  },
  "engines": {
    "node": "10.12"
  },
  "dependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "babel-preset-gatsby-package": "^0.1.3",
    "cross-env": "^5.1.4",
    "sash": "0.0.4"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "slug",
    "URI",
    "directory structure"
  ],
  "main": "add-slug.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/act-labs/gatsby-plugin-relative"
  }
}
